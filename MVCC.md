学习MVCC之前,来看为什么需要他

上篇文章有讲到 ACID,重点是 隔离性,如果没有隔离会出现什么问题

1. 脏读
![[10.png]]
这里 事务2 已经基于 已扣款 也就是 余额 =1000 - 500 = 500 去做决策了,但是 A 的余额=1000

更严重的例子:
```
事务A 初始余额1000,执行了 update 余额 = 10000,没有commit
事务B 读到余额 = 10000,允许取款8000
事务A 回滚,余额还是1000
后果:银行少8000块
```

2. 不可重复读
![[11.png]]
这里,同一事物内,同一行数据前后两次读的值不同,这是不允许的

更严重一点的例子:
```
第一步:select stock from products where id=1 ->查出来 stock=10
判断 10> 0,够卖
第二步:update stock = stock -1 where id=1

但是,这两步中间有人插进来了,给 stock 改成了 0
->你依旧基于 stock=10 做决策 -> 卖超了
```

3. 幻读
![[12.png]]
这里,并不是某一行的值变了,而是符合条件的行数变了

更严重的例子:
```
第一步：SELECT COUNT(*) FROM admins WHERE role='super' → 1 人
判断:只有一个超级管理员

有人 insert 了一个新的 super admin

第二步:执行某个"只允许唯一超管操作"的逻辑
-> 实际有两个超管,安全假设被打破
```

那么由此知道了隔离性的重要性,那么 PostgreSQL 支持四个级别
1. Read Uncommitted(读未提交)
2. Read Committed(读已提交)
3. Repeatable Read(可重复读)
4. Serializable(串行化)

他们的安全性和性能成反比:
![[13.png]]

首先 PostgreSQL 的默认级别是 Read Committed(读已提交),其次,你可以设置 Read Uncommitted,但是 PostgreSQL 会自动升级到 Read Committed


那么Read Committed 到底什么怎么做的?

规则只有一条:每条 SQL 语句执行的瞬间，只能看到在那一瞬间之前已经 COMMIT 的数据
![[14.png]]


那么 Repeatable Read 怎么防住不可重复读?

规则变了一条:整个事务从 BEGIN 那一刻起，看到的都是 BEGIN 时的快照，无论别人后来 COMMIT 了什么
![[15.png]]

但是 Repeatable Read 并没有锁住任何东西,也没有阻止别人修改,别的事务照样可以 insert update delect,完全不受影响

那怎么做到"两次读的结果一样的"的?

那么 MVCC 终于出来了,他解决了 并发读写的根本矛盾

数据库面对两个对立需求:
- 读的想要稳定的数据 -- 别再我看的时候改
- 写的人想要马上改 -- 别让我等

如果用 锁 来解决,写的时候锁住,读的人排队等,这是很安全,但慢的不可接受

MVCC = 多版本并发控制 (Multi-Version Concurrency Control)

解决方法就是:给数据拍快照

做法如下:
1. 先有一个原始数据:

![[16.png]]
2. 执行 update price = 200 (旧版本标记删除 + 插入新版本)
![[17.png]]

两个版本同时存在,这就是 多版本 的意思

由快照决定你看到哪个版本
```text
事务 75(begin 在 txn 80 之前开始) 
->快照冻结点 txn 80 之前 
->看到 price = 100

事务 90(begin 在 txn80 commit 之后)
->快照冻结点 txn 80 之后
->看到price =200
```


锁和死锁

在MVCC不够用的情况下,还是要用到锁,比如:

>写并发--两个事务同时要改同一行

那 PostgreSQL 就必须用 锁 了


![[18.png]]


死锁:互相等对方

![[19.png]]

那么怎么处理?难不成一直等下去?

不会,PostgreSQL 有个死锁检测器(默认每秒检查一次):
- 发现A 和 B形成等待环路
- 选择一个 事务 直接解开锁(通常选代价较小的事务)
- 强制 rollback 该事务
- 另一个事务继续执行

那我们如何避免死锁的出现?

先死锁是怎么形成的
```
事务A：先锁 id=1，再锁 id=2
事务B：先锁 id=2，再锁 id=1
```

可以看出,事务B反过来先给 id=2 给锁住了

如果是这样做的话
```
事务A：先锁 id=1，再锁 id=2
事务B：先锁 id=1，再锁 id=2
```

事务B要先等事务A是否id=1的锁,不会交叉

所以答案就是:所有事务按固定顺序访问资源（比如按 id 从小到大）。顺序一致，环路就不可能形成